pipeline {
  agent none
  stages {
    stage('Compile') {
      steps {
        build 'Projeto primeira fase'
      }
    }

    stage('Tests') {
      steps {
        build 'Testes projeto'
      }
    }

  }
}